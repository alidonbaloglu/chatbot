# Copy to .env and set your keys (leave blank in repo)
# Choose provider: 'openai' or 'gemini'
PROVIDER=gemini

# OpenAI (if PROVIDER=openai)
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o-mini

# Gemini (if PROVIDER=gemini)
GEMINI_API_KEY=
GEMINI_MODEL=gemini-1.5-flash

PORT=5280

# Python scripts (optional)
GOOGLE_API_KEY=
