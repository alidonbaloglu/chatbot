// Minimal stub to satisfy tsconfig include and imports.
// No JSX used to avoid React type dependency.
export type ChatWidgetProps = Record<string, never>;

const ChatWidget = (_props: ChatWidgetProps) => {
  return null; // Implement later
};

export default ChatWidget;
export { ChatWidget };
